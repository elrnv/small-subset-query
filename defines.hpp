#pragma once
#include <vector>
#include <unordered_set>
#include <set>
#include <unordered_map>
#include <utility>

using std::pair;
using std::vector;
using std::unordered_set;
using std::set;
using std::unordered_map;

typedef pair<int,int> pi;
typedef unordered_set<int> seti;
typedef set<int> sseti;
typedef unordered_map<int,int> mapi;
typedef unordered_set<pi> setpi;
typedef vector<int> vi;
typedef pair<vi,vi> pvi;
typedef vector<vi> vvi;
typedef vector<vector<pi>> vvpi;
typedef vector<setpi> vspi;
typedef vector<seti> vsi;
typedef vector<sseti> vssi;
typedef vector<mapi> vmi;
